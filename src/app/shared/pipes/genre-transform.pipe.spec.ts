import { GenreTransformPipe } from './genre-transform.pipe';

describe('GenreTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new GenreTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
